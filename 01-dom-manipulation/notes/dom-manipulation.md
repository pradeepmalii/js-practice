"The DOM is a tree-like representation of the HTML document created by the browser. JavaScript interacts with it through the document object. Any change to the DOM is immediately reflected on the page — that's how dynamic websites work."

****SELECTING ELEMENTS****
5 ways to select the element 
by ID
   Class
   tag
   querySelector
   querySelectorAll


toggle is particularly useful for things like dark mode, dropdowns, and hamburger menus.


   

