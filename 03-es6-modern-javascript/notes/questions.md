1. What is optional chaining and when would you use it?
Optional chaining (?.) is a feature that lets you safely access properties of an object without causing errors if something is missing.Use it When Data May Be Missing (APIs / JSON)

2. What does user?.address?.city return if address is undefined?
it will return default.

3. What's the difference between || and ?? ?
|| = “give me first truthy value”
?? = “give me first non-null/undefined value”

